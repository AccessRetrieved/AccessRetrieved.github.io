function alertDownload(language) {
    if language == true {
        alert("App Postman暂时直供IOS客户端，网页版暂不支持。");
    } else {
        alert("App Postman only works with IOS client, web app not supported.");
    }
}